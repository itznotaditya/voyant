# Backend Environment Variables
# Currently no environment variables are required for the backend
# All APIs used are free and don't require API keys

# Optional: Set a custom port (default is 8000)
# PORT=8000
